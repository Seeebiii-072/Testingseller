{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd app/backend && npm ci"
  },
  "deploy": {
    "startCommand": "cd app/backend && npm start",
    "healthcheckPath": "/api/payment/test",
    "healthcheckTimeout": 300
  }
}
